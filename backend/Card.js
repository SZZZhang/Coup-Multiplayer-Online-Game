class Card {

    //counter actions
    blockForeignAid(){
        
    }

    //takes in Player obj
    income(player) {
        player.coins++; 
    }

    //
    foreignAid(player) {
        counterAction

        player.coins += 2; 
    }
}